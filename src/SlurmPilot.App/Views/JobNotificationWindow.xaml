<Window
    x:Class="SlurmPilot.App.Views.JobNotificationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Width="380"
    Height="132"
    WindowStyle="None"
    AllowsTransparency="True"
    ResizeMode="NoResize"
    ShowActivated="False"
    ShowInTaskbar="False"
    Topmost="True"
    Background="Transparent">

    <Border x:Name="RootCard"
            Background="{DynamicResource BgPanelBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="14"
            Cursor="Hand"
            MouseLeftButtonUp="RootCard_MouseLeftButtonUp"
            Opacity="0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18"
                              Direction="270"
                              Opacity="0.22"
                              ShadowDepth="6"
                              Color="#000000" />
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border x:Name="IconHost"
                    Width="34"
                    Height="34"
                    CornerRadius="17"
                    Margin="0,2,12,0"
                    VerticalAlignment="Top"
                    Background="{DynamicResource BgSurface0Brush}">
                <TextBlock x:Name="IconText"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="16"
                           FontWeight="SemiBold" />
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock x:Name="TitleText"
                           Grid.Row="0"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="MessageText"
                           Grid.Row="1"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="12"
                           TextWrapping="Wrap"
                           MaxHeight="68"
                           Margin="0,5,0,0" />
                <Grid Grid.Row="2" Margin="0,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{DynamicResource Monitor.NotificationClickHint}"
                               Foreground="{DynamicResource TextMutedBrush}"
                               FontSize="11"
                               VerticalAlignment="Center" />
                    <Button Grid.Column="1"
                            Content="{DynamicResource Monitor.NotificationMarkAllRead}"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="{DynamicResource TextMutedBrush}"
                            FontSize="11"
                            Padding="6,1"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            Click="MarkAllReadButton_Click" />
                </Grid>
            </Grid>

            <Button Grid.Column="2"
                    Content="✕"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{DynamicResource TextMutedBrush}"
                    FontSize="11"
                    Padding="6,2"
                    Margin="8,-2,-4,0"
                    VerticalAlignment="Top"
                    Click="CloseButton_Click" />
        </Grid>
    </Border>
</Window>
