<Window
    x:Class="SlurmJobManager.App.Views.RemoteFileEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:conv="clr-namespace:SlurmJobManager.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{Binding WindowTitle}"
    Height="540" Width="680"
    MinHeight="320" MinWidth="440"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    AllowsTransparency="False"
    ResizeMode="CanResize"
    Background="{DynamicResource BgBaseBrush}"
    Foreground="{DynamicResource TextPrimaryBrush}">

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="36"
            ResizeBorderThickness="5"
            GlassFrameThickness="0,0,0,1"
            CornerRadius="0" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
        <conv:StyleKeyToStyleConverter x:Key="StyleKeyToStyle" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource BgPanelBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid>
                <Border Background="Transparent"
                        HorizontalAlignment="Stretch"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" />
                <TextBlock Text="{Binding WindowTitle}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           FontSize="13"
                           FontWeight="SemiBold"
                           IsHitTestVisible="False" />
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top">
                    <Button x:Name="BtnMinimize"
                            Style="{StaticResource TitleBarButtonStyle}"
                            ToolTip="{DynamicResource TitleBar.Minimize}"
                            Click="BtnMinimize_Click">
                        <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10" />
                    </Button>
                    <Button x:Name="BtnClose"
                            Style="{StaticResource TitleBarCloseButtonStyle}"
                            ToolTip="{DynamicResource TitleBar.Close}"
                            Click="BtnCloseChrome_Click">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{Binding FileName}"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="13" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding DisplayRemotePath}"
                                 Foreground="{DynamicResource TextMutedBrush}"
                                 FontFamily="Consolas" FontSize="11"
                                 ToolTip="{Binding RemotePath}"
                                 TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding EditorStateText}"
                               Foreground="{DynamicResource TextMutedBrush}"
                               FontSize="11" />
                    <TextBlock Text="{Binding LargeFileModeText}"
                               Foreground="{DynamicResource AccentYellowBrush}"
                               FontSize="11"
                               TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{DynamicResource BgSurface1Brush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="8,3"
                            Margin="0,0,6,0"
                            ToolTip="{DynamicResource RemoteEditor.EncodingTip}">
                        <TextBlock Text="{Binding EncodingName}"
                                   FontFamily="Consolas"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="11" />
                    </Border>
                    <Border Background="{DynamicResource BgSurface1Brush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="8,3"
                            ToolTip="{Binding FileSizeBytes}">
                        <TextBlock Text="{Binding FileSizeText}"
                                   FontFamily="Consolas"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="11" />
                    </Border>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        Margin="0,0,0,6"
                        IsEnabled="{Binding CanReload}"
                        Visibility="{Binding IsStructuredModeAvailable, Converter={StaticResource BoolToVis}}">
                <Button Content="{DynamicResource RemoteEditor.StructuredMode}"
                        Command="{Binding SwitchToStructuredModeCommand}"
                        Style="{StaticResource NeutralButtonStyle}"
                        Padding="8,3"
                        Margin="0,0,6,0" />
                <Button Content="{DynamicResource RemoteEditor.TextMode}"
                        Command="{Binding SwitchToTextModeCommand}"
                        Style="{StaticResource NeutralButtonStyle}"
                        Padding="8,3" />
            </StackPanel>

            <!-- Editor -->
            <TextBlock Grid.Row="2"
                       Text="{DynamicResource RemoteEditor.BinaryRejected}"
                       Style="{StaticResource WarningTextStyle}"
                       FontSize="12"
                       Margin="0,0,0,6"
                       Visibility="{Binding IsBinaryFile, Converter={StaticResource BoolToVis}}" />

            <Border Grid.Row="3"
                    Background="{DynamicResource BgCrustBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsStructuredModeAvailable}" Value="True" />
                                    <Condition Binding="{Binding IsStructuredMode}" Value="True" />
                                    <Condition Binding="{Binding IsBinaryFile}" Value="False" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding StructuredItems}" Margin="8">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="3*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                        <TextBlock Text="{Binding Key}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Section}"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   FontSize="11" />
                                    </StackPanel>
                                    <Grid Grid.Column="1">
                                         <TextBox Text="{Binding StringValue, UpdateSourceTrigger=PropertyChanged}">
                                             <TextBox.Style>
                                                 <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                                     <Setter Property="Visibility" Value="Collapsed" />
                                                     <Style.Triggers>
                                                         <DataTrigger Binding="{Binding IsString}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBox.Style>
                                        </TextBox>
                                         <CheckBox IsChecked="{Binding BoolValue, Mode=TwoWay}">
                                             <CheckBox.Style>
                                                 <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                                     <Setter Property="Visibility" Value="Collapsed" />
                                                     <Style.Triggers>
                                                         <DataTrigger Binding="{Binding IsBoolean}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </CheckBox.Style>
                                        </CheckBox>
                                         <TextBox Text="{Binding IntegerValue, UpdateSourceTrigger=LostFocus}">
                                             <TextBox.Style>
                                                 <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                                     <Setter Property="Visibility" Value="Collapsed" />
                                                     <Style.Triggers>
                                                         <DataTrigger Binding="{Binding IsInteger}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBox.Style>
                                        </TextBox>
                                         <TextBox Text="{Binding FloatingValue, UpdateSourceTrigger=LostFocus}">
                                             <TextBox.Style>
                                                 <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                                     <Setter Property="Visibility" Value="Collapsed" />
                                                     <Style.Triggers>
                                                         <DataTrigger Binding="{Binding IsFloating}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBox.Style>
                                        </TextBox>
                                        <TextBlock Text="{DynamicResource RemoteEditor.NullValue}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsNull}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                    <Button Grid.Column="2"
                                            Content="..."
                                            Command="{Binding BrowseValueCommand}"
                                            Margin="6,0,0,0"
                                            Padding="8,2">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource NeutralButtonStyle}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsString}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <Border Grid.Row="3"
                    Background="{DynamicResource BgCrustBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsTextMode}" Value="True" />
                                    <Condition Binding="{Binding IsBinaryFile}" Value="False" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <avalonedit:TextEditor x:Name="Editor"
                                       ShowLineNumbers="True"
                                       FontFamily="Consolas"
                                       FontSize="13"
                                       Background="{DynamicResource BgCrustBrush}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       IsReadOnly="{Binding IsBusy}"
                                       WordWrap="False"
                                       HorizontalScrollBarVisibility="Auto"
                                       VerticalScrollBarVisibility="Auto" />
            </Border>

            <!-- Status -->
            <TextBlock Grid.Row="4"
                       Text="{Binding StatusMessage}"
                       Style="{Binding StatusStyleKey, Converter={StaticResource StyleKeyToStyle}}" />

            <!-- Buttons -->
            <Grid Grid.Row="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{DynamicResource RemoteEditor.Saving}"
                           Style="{StaticResource BusyTextStyle}"
                           VerticalAlignment="Center"
                           Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />

                <Button Grid.Column="1" Content="{DynamicResource RemoteEditor.Save}"
                        IsEnabled="{Binding CanSave}"
                        Click="BtnSave_Click"
                        Style="{StaticResource AccentButtonStyle}"
                        Padding="12,5" Margin="0,0,6,0" />
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="{DynamicResource RemoteEditor.Reload}"
                            IsEnabled="{Binding CanReload}"
                            Style="{StaticResource NeutralButtonStyle}"
                            Padding="12,5"
                            Margin="0,0,6,0"
                            Click="BtnReload_Click" />
                    <Button Content="{DynamicResource RemoteEditor.Close}"
                            Style="{StaticResource NeutralButtonStyle}"
                            Padding="12,5"
                            Click="BtnClose_Click" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
