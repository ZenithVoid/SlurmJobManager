<Window
    x:Class="SlurmJobManager.App.Views.Dialogs.ConfirmationDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:SlurmJobManager.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{Binding Title}"
    Width="560"
    MinWidth="500"
    MinHeight="260"
    Height="360"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    Background="{DynamicResource BgBaseBrush}">
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="{Binding IconText}"
                       FontSize="20"
                       Margin="0,0,10,0"
                       VerticalAlignment="Top"
                       Foreground="{DynamicResource AccentYellowBrush}" />
            <TextBlock Grid.Column="1"
                       Text="{Binding Message}"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       FontSize="13"
                       TextWrapping="Wrap" />
        </Grid>

        <Border Grid.Row="2"
                Background="{DynamicResource BgCrustBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="10"
                Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding Details}"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextMutedBrush}"
                           FontFamily="Consolas"
                           FontSize="12" />
            </ScrollViewer>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Details}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="{Binding CancelButtonText}"
                    Style="{StaticResource NeutralButtonStyle}"
                    Padding="12,5"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Click="BtnCancel_Click"
                    Visibility="{Binding ShowCancelButton, Converter={StaticResource BoolToVis}}" />
            <Button Content="{Binding DiscardButtonText}"
                    Style="{StaticResource NeutralButtonStyle}"
                    Padding="12,5"
                    Margin="0,0,8,0"
                    Click="BtnDiscard_Click"
                    Visibility="{Binding ShowDiscardButton, Converter={StaticResource BoolToVis}}" />
            <Button Content="{Binding ConfirmButtonText}"
                    Style="{StaticResource DangerButtonStyle}"
                    Padding="12,5"
                    IsDefault="True"
                    Click="BtnConfirm_Click" />
        </StackPanel>
    </Grid>
</Window>
