<Window
    x:Class="SlurmJobManager.App.Views.Dialogs.TaskBlueprintSaveView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{DynamicResource Task.BlueprintSaveTitle}"
    Width="480"
    Height="300"
    MinWidth="420"
    MinHeight="260"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource BgBaseBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{DynamicResource Task.BlueprintName}"
                   Style="{StaticResource FieldLabelStyle}" />
        <TextBox Grid.Row="1"
                 Text="{Binding BlueprintName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,8" />

        <TextBlock Grid.Row="2"
                   Text="{DynamicResource Task.BlueprintDescription}"
                   Style="{StaticResource FieldLabelStyle}" />
        <TextBox Grid.Row="3"
                 Text="{Binding BlueprintDescription, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

        <TextBlock Grid.Row="4"
                   Text="{Binding StatusMessage}"
                   Style="{StaticResource WarningTextStyle}"
                   Margin="0,8,0,8"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="{DynamicResource Btn.Save}"
                    Style="{StaticResource SuccessButtonStyle}"
                    Padding="12,5"
                    Margin="0,0,8,0"
                    Click="BtnSave_Click" />
            <Button Content="{DynamicResource Btn.Cancel}"
                    Style="{StaticResource NeutralButtonStyle}"
                    Padding="12,5"
                    Click="BtnCancel_Click" />
        </StackPanel>
    </Grid>
</Window>
