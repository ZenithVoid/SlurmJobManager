<UserControl
    x:Class="SlurmJobManager.App.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:SlurmJobManager.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="700" d:DesignWidth="900"
    Background="{DynamicResource BgBaseBrush}">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,4,0">
        <StackPanel Margin="24,20,24,20">

            <!-- Page title -->
            <TextBlock Text="{DynamicResource Dashboard.Title}"
                       Style="{StaticResource PageTitleStyle}" />

            <!-- Row 1: Connection + Quick Actions -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Connection Status card -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.Connection}"
                                   Style="{StaticResource SectionHeaderStyle}" />

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Ellipse x:Name="StatusDot" Width="10" Height="10"
                                     VerticalAlignment="Center" Margin="0,0,8,0">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{DynamicResource AccentRedBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                                <Setter Property="Fill"
                                                        Value="{DynamicResource AccentGreenBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{Binding ConnectionStatusText}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="14" FontWeight="SemiBold"
                                       VerticalAlignment="Center" />
                        </StackPanel>

                        <TextBlock Text="{Binding ConnectionStatusMessage}"
                                   Style="{StaticResource MutedTextStyle}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <!-- Quick Actions card -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.QuickActions}"
                                   Style="{StaticResource SectionHeaderStyle}" />

                        <Button Content="{DynamicResource Btn.TestConnection}"
                                Command="{Binding QuickTestCommand}"
                                Style="{StaticResource AccentButtonStyle}"
                                HorizontalAlignment="Stretch"
                                Margin="0,0,0,8" Padding="10,7" />

                        <Button Content="{DynamicResource Btn.RefreshMonitor}"
                                Command="{Binding QuickRefreshCommand}"
                                Style="{StaticResource NeutralButtonStyle}"
                                HorizontalAlignment="Stretch"
                                Margin="0,0,0,8" Padding="10,7" />

                        <Button Content="{DynamicResource Btn.GoToTasks}"
                                Command="{Binding NavigateToTasksCommand}"
                                Style="{StaticResource SuccessButtonStyle}"
                                HorizontalAlignment="Stretch"
                                Padding="10,7" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Row 2: Job Stats cards -->
            <TextBlock Text="{DynamicResource Dashboard.JobSummary}"
                       Style="{StaticResource SectionHeaderStyle}"
                       Margin="0,0,0,8" />

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Total -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="12,14">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{Binding TotalJobs}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="28" FontWeight="Bold"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="{DynamicResource Dashboard.Total}"
                                   Style="{StaticResource MutedTextStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </Border>

                <!-- Running -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="12,14">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{Binding RunningJobs}"
                                   Foreground="{DynamicResource StateRunningBrush}"
                                   FontSize="28" FontWeight="Bold"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="{DynamicResource Dashboard.Running}"
                                   Style="{StaticResource MutedTextStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </Border>

                <!-- Pending -->
                <Border Grid.Column="4" Style="{StaticResource CardStyle}" Padding="12,14">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{Binding PendingJobs}"
                                   Foreground="{DynamicResource StatePendingBrush}"
                                   FontSize="28" FontWeight="Bold"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="{DynamicResource Dashboard.Pending}"
                                   Style="{StaticResource MutedTextStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </Border>

                <!-- Completed -->
                <Border Grid.Column="6" Style="{StaticResource CardStyle}" Padding="12,14">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{Binding CompletedJobs}"
                                   Foreground="{DynamicResource StateCompletedBrush}"
                                   FontSize="28" FontWeight="Bold"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="{DynamicResource Dashboard.Completed}"
                                   Style="{StaticResource MutedTextStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </Border>

                <!-- Failed -->
                <Border Grid.Column="8" Style="{StaticResource CardStyle}" Padding="12,14">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{Binding FailedJobs}"
                                   Foreground="{DynamicResource StateFailedBrush}"
                                   FontSize="28" FontWeight="Bold"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="{DynamicResource Dashboard.Failed}"
                                   Style="{StaticResource MutedTextStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Monitor status -->
            <Border Style="{StaticResource CardStyle}" Padding="12,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{DynamicResource Dashboard.Monitor}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding MonitorStatusMessage}"
                               Foreground="{DynamicResource TextMutedBrush}"
                               FontSize="12" VerticalAlignment="Center" />
                    <Button Content="{DynamicResource Btn.OpenMonitor}"
                            Command="{Binding NavigateToMonitorCommand}"
                            Style="{StaticResource GhostButtonStyle}"
                            Foreground="{DynamicResource AccentBlueBrush}"
                            Margin="12,0,0,0" Padding="0" FontSize="12" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
